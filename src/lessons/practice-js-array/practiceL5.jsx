// ⚫ 도전 — 빈 화면에서 처음부터. 데이터도 직접 두고, 재고 필터 + 세일가 목록을 만든다.
// 목표(정답): 재고 있는 상품만, 이름 — (price * 0.9)원 (재고 N) 형태로 <li> 목록.

export default function PracticeL5() {
  // TODO 1: 상품 배열 (name·price·stock 몇 개)
  // TODO 2: 재고 있는 것만 filter
  // TODO 3: <ul> 안에 map으로 <li key=…> 목록 (세일가 = price * 0.9)
  return (
    <div className="demo-card" style={{ padding: 12 }}>여기에 상품 목록을 만들자</div>
  )
}
