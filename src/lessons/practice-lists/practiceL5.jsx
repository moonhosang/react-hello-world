// ⚫ 도전 — 껍데기만 있다. 태그 목록을 처음부터 만든다.
// TODO A: import { useState } 후 tags 배열 state와 text 입력 state를 만든다.
// TODO B: add 함수 — 입력값을 tags에 추가([...tags, 입력값])하고 입력을 비운다.
// TODO C: 입력창 + 추가 버튼 + 목록(map으로 <li key={...}>) + 빈 목록 안내를 그린다.
//   막히면 👀 정답 보기로 비교한다.

export default function PracticeL5() {
  return (
    <div className="demo-card" style={{ padding: 12 }}>
      여기에 태그 목록을 처음부터 만들자
    </div>
  )
}
