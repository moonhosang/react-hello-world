# 📋 커리큘럼 백로그 (나중에 추가할 것)

> 대화 중 나온 "언젠가 추가할 것"을 여기 모은다. 흩어지지 않게.
> 우선순위: 🔴 높음 · 🟡 중간 · ⚪ 낮음/선택 · ✅ 완료

---

## 🔴 높음

- [x] **JS 기본 워밍업 (별도 🟨 JS 트랙)** — ✅ 6강 완료(J1 표현식 해부 · J2 화살표·삼항·템플릿 · J3 truthy/falsy · J4 배열 map/filter · J5 구조분해·스프레드 · J6 async/await). 트랙 탭으로 React와 분리.
- [ ] ~~JS 기본 워밍업 (원안)~~ — 우리 레슨 다수가 이미 아는 것으로 전제하는 JS를 짧게 짚는다.
  - 배열 메서드 `map`·`filter` (리스트 렌더링 전제)
  - 구조 분해 `const {a} = obj` / `const [x] = arr` (props·useState 전제)
  - 스프레드 `{...obj}` / `[...arr]` (불변성 업데이트 전제)
  - 화살표 함수 · **삼항 연산자**(JSX에서 매우 자주 씀) · 템플릿 리터럴
  - truthy/falsy (조건부 렌더링 `&& 0` 함정과 연결)
  - `Promise` / `async·await` (useEffect 데이터 불러오기 전제)
  - → 각 항목마다 "이건 어느 리액트 레슨에서 쓰이나" 링크

- [ ] **표현식(expression) 해부 — 기초 중의 기초** — 1.46(JSX 규칙)·4.5(조건부 렌더링)의 공통 뿌리.
  - **왜 배우나(핵심 동기)**: 학문이 아니라 **실전 기술**이다 — 복잡한 코드 덩어리(중첩 삼항, `a?.b && c.map(...)` 등)를 **디버깅**하거나 **세밀히 읽을 때**, 큰 식을 서브식으로 쪼개 "어디서 값이 틀어지나"를 짚는 능력.
  - 라이브(권장): **버그가 든 복잡한 식**을 서브식 단위로 분해해 각 조각의 값을 확인하며 범인을 찾아가는 데모 (실제 디버깅 흐름 재현).
  - **표현식(값이 되는 것) vs 문장(동작)** 구분 → JSX `{}` 안엔 표현식만, `if`는 왜 안 되나
  - **서브식 단위로 분해해 읽기**: `count > 0 && <Badge/>` → `count>0` 먼저 평가 → 그다음 `&&`
  - 연산자 우선순위·평가 순서(왼→오, 단축 평가) → `&&`/`||`가 반환하는 값, `&& 0` 함정의 근원
  - **용어**: 항(term)·피연산자(operand)·연산자(operator)·식(expression)·문(statement) — 문서·에러 메시지를 읽으려면 필수
  - 라이브: 식을 넣으면 서브식별 값을 단계로 펼쳐 보여주는 "표현식 해부기" 데모
  - → 완료 후 1.46·4.5에서 이 레슨으로 역참조 링크

- [ ] **용어 사전 (glossary)** — 커리큘럼 전반의 은근히 어려운 말들을 한 곳에 모아 한 줄 정의 + 쓰인 레슨 링크.
  - JS: 표현식·문·항·피연산자·연산자·truthy/falsy·콜백·Promise·구조분해·스프레드
  - React: props·state·render·리렌더·mount/unmount·hook·effect·dependency(의존성)·dispatch·reducer·controlled/uncontrolled·단방향 데이터 흐름·합성(children)·인스턴스
  - 각 레슨에서 처음 나온 용어에 이 사전으로 링크(용어 클릭 → 정의로 점프)

- [ ] **8-1 분리 (과적 해소)** — 지금 8-1이 intro치고 너무 무겁다.
  - 8-1 · useEffect 소개 = `① 마운트 · ② 의존성 · ④ useUserFetch`
  - 8-1b · 왜/어떻게 = `②-2 렌더 순수성 · ③ 동기화 · ③-2 유용성 · ③-3 without-effect · ⑤ 완성해보기`
  - (구조·네비 변경이라 진행 전 확인 필요)

## 🟡 중간

- [ ] **클래스 컴포넌트 & 에러 바운더리 정식 소개** — 데모에서 쓰고 있는데(에러 바운더리) 정작 안 가르침. "지금은 함수형이 기본, 에러 바운더리만 아직 클래스" 맥락으로.
- [ ] **React DevTools 사용법** — 컴포넌트 트리·props·state·리렌더 하이라이트 보는 법 (실행 데모보단 스크린샷/설명).
- [ ] **비동기 상태 3형태 패턴** — loading / error / success를 한 곳에서 다루는 표준 형태 (8단계 확장, useUserFetch에 error 추가).

## ⚪ 낮음 / 선택

- [ ] **스타일링 생태계** — styled-components · Tailwind · CSS 모듈 (라이브러리 설치 필요, 메커니즘과 별개라 별도 트랙).
- [ ] **폰트·이미지 렌더링** — public vs src, `@font-face` (책 5장).
- [ ] **커스텀 훅 심화** — 여러 커스텀 훅 조합 / 훅 합성 패턴.
- [ ] **라우팅 맛보기** — react-router (범위가 커서 입문 후반 or 별책).

---

## 🧰 앱 기능 (커리큘럼 내용 아님)

- [x] **URL 해시 동기화** — 새로고침 위치 유지·링크 공유·뒤로/앞으로 (react-router 없이, `#8.1`)
- [x] **진도 체크박스 + 진행률 바** — 항목 옆 체크박스, `localStorage` 저장, "N/46 · %"
- [x] **이어서 학습하기** — 해시 없으면 마지막 위치로 복원
- [ ] **검색 / 필터** — 주제로 바로 점프
- [ ] **사이드바 접기** — 좁은 화면·집중 모드
- (제외) 다크모드 토글, 이전/다음 버튼

## ✅ 완료 (기록용)

- [x] JSX 문법 규칙 6가지 (1.46)
- [x] children / 컴포넌트 합성 (2-3)
- [x] 이벤트 전파 & 전개 props (3.3)
- [x] 조건부 렌더링 3패턴 + 0 함정 (4.5)
- [x] 8-1 useEffect: 동기화·유용성·without-effect·완성해보기·5-5 연결
- [x] 4단계 입력: useRef 제거 → 그냥 두기 vs controlled + 단방향 데이터 흐름
